|操作符|说明|
|:-|:-|
|TRIM()|去掉字符串左右两边的空格|
|LTrim()|去掉字符串左边的空格|
|RTrim()|去掉字符串右边的空格|
|CONCAT()|拼接字符串|
|LENGTH()|返回字符串长度|
|LOWER()|将字符串转换为小写|
|UPPER()|将字符串转换为大写|
|REPLACE()|替换字符串|
|SUBSTRING_INDEX()|分隔符分隔的子串索引|
